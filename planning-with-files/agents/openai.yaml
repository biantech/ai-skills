interface:
  display_name: "Planning with Files"
  short_description: "Persist plans, findings, and progress across sessions"
  default_prompt: "Use $planning-with-files to organize this substantial task with durable planning files."
policy:
  allow_implicit_invocation: true
