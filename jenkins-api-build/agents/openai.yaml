interface:
  display_name: "Jenkins API Build"
  short_description: "Inspect and run approved Jenkins build workflows"
  default_prompt: "Use $jenkins-api-build to inspect or run the explicitly requested Jenkins job. Return queue details immediately after triggering unless the user asks to query or wait for the result."
policy:
  allow_implicit_invocation: true
