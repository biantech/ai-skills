interface:
  display_name: "Azure App Insights query"
  short_description: "Read-only KQL logs through Azure CLI on 237, via 237 machine"
  default_prompt: "Use $azure-appinsights-query to run a bounded, read-only KQL query through the authenticated Azure CLI on server237."
policy:
  allow_implicit_invocation: true
