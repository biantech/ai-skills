interface:
  display_name: "Graphify"
  short_description: "Build and explore local knowledge graphs"
  default_prompt: "Use $graphify to build or query a knowledge graph for this workspace."
policy:
  allow_implicit_invocation: true
