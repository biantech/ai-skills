interface:
  display_name: "Fact-first Debugging"
  short_description: "Separate code-proven findings from runtime evidence"
  default_prompt: "Use $fact-first-diagnose to separate logic-provable conclusions from runtime-dependent claims and identify the minimum evidence needed."
policy:
  allow_implicit_invocation: true
